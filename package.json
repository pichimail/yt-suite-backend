{
  "name": "yt-suite-backend",
  "version": "1.0.0",
  "description": "Backend service for YouTube downloader & processor",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "dev": "nodemon server.js",
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "echo \"No build step needed\"",
    "gcp:deploy": "gcloud run deploy yt-suite-backend --source . --region us-central1 --allow-unauthenticated"
  },
  "keywords": ["youtube", "downloader", "yt-dlp", "cloud-run", "video", "audio"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "archiver": "^7.0.1",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}